syntax = "proto3";

package topic2.v2;

// Updated schema for v2 (or test2) of topic2
message Data {
  string id = 1;
  repeated Data2 related_data = 2; // Reference to the Data2 message from topic2 v1
  bool is_active = 3;
}